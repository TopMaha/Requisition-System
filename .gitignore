# --- Secrets & local diagnostic dumps (NEVER commit) ---
cf_token.txt
tokens.txt
cfconfig.txt
*.vbs
deploy_log.txt
sub_enable_result.json
sub_info_result.json

# Local credential/token caches
*.token
.dev.vars
.wrangler/

# Node
node_modules/
npm-debug.log*

# OS / editor
Thumbs.db
.DS_Store
*.swp
wrangler.toml

# Local tooling
.claude/

# Large local data / archives (exceed GitHub 100MB limit — never deploy via Pages)
*.7z
*.xlsx
Inven/
